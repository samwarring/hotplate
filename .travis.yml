language: cpp

cache:
  directories:
    - vcpkg

os:
  - linux
  - osx

dist: xenial

compiler:
  - gcc
  - clang

env:
  - CONFIG=Debug
  - CONFIG=RelWithDebInfo

matrix:
  exclude:
    - os: linux
      compiler: clang
    - os: osx
      compiler: gcc

addons:
  apt:
    packages:
      - libboost-all-dev
      - g++-7
  homebrew:
    packages:
      - gcc6
	  - gcc7

before_script:
  - scripts/configure_travis.sh .

script:
  - cmake --build .
  - ctest
